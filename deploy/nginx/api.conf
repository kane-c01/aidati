# ====================================================
# Nginx - 业务后端反向代理(占位)
# 文档:开发文档/05-部署运维与安全.md §四
# 完整 Nginx 配置(SSL / WAF / 限流 / 结构化日志)由 M9 里程碑实现
# ====================================================

# server {
#     listen 443 ssl http2;
#     server_name api.yourdomain.com;
#     ...
# }

# 占位
server {
    listen 8080;
    server_name _;
    location = /healthz {
        return 200 'nginx placeholder';
        add_header Content-Type text/plain;
    }
}
