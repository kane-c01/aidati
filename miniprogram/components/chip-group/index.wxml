<view class="chip-group">
  <view
    wx:for="{{options}}"
    wx:key="value"
    class="chip {{ activeMap[item.value] ? 'chip--active' : '' }} {{ item.disabled ? 'chip--disabled' : '' }}"
    data-value="{{item.value}}"
    bindtap="onTap"
  >
    <text>{{item.label}}</text>
  </view>
</view>
